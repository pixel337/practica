//Калькуляторы фигур на JavaScript
//1. Сделайте калькулятор, который будет находить площадь и периметр квадрата.
/*function square(slength){
    return slength*slength;
}

function perimeter(slength){
    return slength * 4;
}

const slength = 4;
const sqarea = square(slength);
const sperimeter = perimeter(slength);
console.log(`Площадь квадрата: ${sqarea}`);
console.log(`Периметр квадрата: ${sperimeter}`);

//2. Сделайте калькулятор, который будет находить площадь и периметр прямоугольника.

function rarea(length, width){
    return length * width;
}

function rperimeter(length, width){
    return 2 * (length + width);
}

const length = 4;
const width = 6;
const area = rarea(length, width);
const perim = rperimeter(length, width);
console.log(`Площадь прямоугольника: ${area}`);
console.log(`Периметр прямоугольника: ${perim}`);


//3. Сделайте калькулятор, который будет находить площадь круга и длину окружности.
const pi = Math.PI;

function carea(radius) {
    return pi * radius * radius;
}

function circumference(radius) {
    return 2 * pi * radius;
}

const radius = 9; 
const area = carea(radius);
const circleircumference = circumference(radius);
console.log(`Площадь круга: ${area}`);
console.log(`Длина окружности: ${circleircumference}`);

//4. Сделайте калькулятор, который будет находить площадь треугольника по трем сторонам.
function trarea(side1, side2, side3) {
    const s = (side1 + side2 + side3) / 2;
    return Math.sqrt(s * (s - side1) * (s - side2) * (s - side3));
}

const side1 = 5; 
const side2 = 7; 
const side3 = 8; 
const trianglear = trarea(side1, side2, side3);
console.log(`Площадь треугольника: ${trianglear}`);
*/
